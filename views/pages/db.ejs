<!DOCTYPE html>
<html>
<head>
  <% include ../partials/header.ejs %>
</head>

<body>

<% include ../partials/nav.ejs %>

<div class="container">
<h2>Database Results</h2>

<ul>
    <% rows.forEach(function(r) { %>
		<li>ID:<%= r.id %> NAME:<%= r.name %> AGE:<%= r.age %> Weight:<%= r.weight %> Height:<%= r.height %> Type:<%= r.type %></li>
    <% }); %>
</ul>

</div>

</body>
</html>
